name: nightshiftos_app
description: NightShiftOS App domain implementation for Flutter.
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  http: ^1.2.2
  permission_handler: ^11.3.1
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^19.4.2
  flutter_secure_storage: ^9.2.4
  supabase_flutter: ^2.8.4
  connectivity_plus: ^7.0.0
  flutter_beacon: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
